import 'package:flutter/material.dart';

class LoginPannel extends StatelessWidget {
  const LoginPannel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [Text("Phone Number")],
      ),
    );
  }
}
